# 《程序设计基础课程设计》题签

**小组成员**：____________________ / ____________________

---

## 正文

编写一个C/C++程序（推荐C++14/17），面向中小型医院/社区卫生服务中心，实现轻量级医院信息管理系统（HIS），动态管理患者档案、医疗记录、病房床位、药品库存及其基本操作。系统核心功能如下：

### 1. 用户的注册，登录控制

a) 用户分为**普通患者、门诊/住院医生、护士/床位管理员、药房药剂师、系统管理员**五类，权限划分如下：

- 患者：预约挂号、查看自身病历/检查报告、查询处方明细；
- 医护：接诊登记、开具检查单/处方、发起住院审批、录入体征数据；
- 药剂师：处方审核、药品出入库、发药确认；
- 管理员：基础操作+基础字典维护、账号解封/封禁、全局数据查询、查看运营报表与盈亏/资源预测。

b) 安全规范：本地配置文件**不存储明文密码**，采用MD5/SHA-256哈希值比对验证用户名与密码。
c) 防暴力破解：用户连续5次输入错误密码，账户自动锁死，锁定密码输入通道，仅管理员可解封。

### 2. 核心信息管理（患者档案、医疗记录、病房床位、药品库存）

a) 数据结构：三类核心业务数据采用**动态内存双向链表（或C++标准容器）** 实现，支持增、删、查、改全功能：

- **患者信息与医疗记录**：包含患者基础信息（ID、姓名、联系方式、过敏史等），强关联**挂号、看诊、检查、住院**4类记录，记录需包含时间戳、主诉、初步诊断、医嘱、检查项目/报告状态、床位分配等字段；
- **住院病房与床位管理**：包含病房类型、关联科室、床位状态（空闲/预约/占用/消毒/维护）、收费标准、当前绑定患者ID，支持状态机流转与跨科室协调；
- **药房与药品管理**：包含药品通用名、商品名、别名、规格、进价、售价、当前库存、安全阈值、有效期、关联科室（专科药标记），支持出入库流水记录与处方发药扣减。

b) 查询功能：支持单个患者/药品/科室**一键下钻查询**历史就诊、处方、检查报告、床位流转记录。
c) 时间规范：录入自动生成**系统时间戳**，程序内部维护严格时间顺序，保障病历追溯与财务对账。
d) 【数据字典与关联约束】：

- 科室与医生：预设≥5个标准科室（内科、外科、儿科、妇产科、急诊科），每科室绑定≥3名医生，支持医生排班、跨科会诊归档；
- 病房类型与科室：≥3种病房（普通、隔离、VIP/特需），急诊/呼吸科优先隔离病房、妇产科专属VIP、儿科绑定普通病房，支持跨科调剂与日志记录；
- 药品分类与科室：≥20类药理分类，区分通用名/商品名/别名，支持动态存储与模糊检索，专科用药触发权限警告/二次审批；
- 容量与重名：容纳≥100名门诊患者、≥30名住院患者，以**患者业务ID/医保号**为唯一主键，动态内存支持超长文本存储，无数据截断。

### 3. 数据文件管理

a) 核心链表（患者病历、病房床位、药品库存）各对应**两份txt数据文件**，分别用于实时修改与安全备份；
b) 采用**二进制.dat文件**序列化存储原始数据，明文文件损坏时自动反序列化恢复，实现数据零丢失。

### 4. 医院运营情况查询

支持分级统计查询，所有结果均以**控制台格式化文本报表/终端对齐输出**形式呈现，覆盖三类视角：

- 管理视角：全院床位利用率、科室接诊量、药品消耗、财务盈亏预测等综合报表；
- 医护视角：个人工作量、待办清单；
- 患者视角：个人费用、就诊时间轴。

### 5. 非法输入检测

所有输入环节添加**类型、范围、长度、格式校验**，拦截越界值、非法字符、空值提交，防止程序崩溃与内存泄漏。

### 6. 特色功能

a) 字符串模糊查询：基于**余弦相似度**实现药品别名、患者姓名拼音首字母的智能检索；
b) 机器学习预测：通过**梯度下降+线性回归**，预测未来7天科室床位需求与营收趋势，控制台输出拟合参数与预测数值；
c) 处方撤销与退药：支持未发处方申诉退款/退药，管理员审核后恢复余额、库存，标记记录无效；
d) 标准化开发：采用MinGW-64/GCC+CMake，模块化拆分`include/src/data/build`目录，提升可移植性；
e) 软件打包：通过Visual Studio Installer/Inno Setup生成.msi/.exe安装包，支持安装与卸载。

### 7. 小组工作流

a) 版本控制：使用Git管理代码，托管至Gitee，答辩前设为公开权限；
b) 代码规范：对所有人的代码格式、文件名、变量名和函数名的命名做出严格规定，禁止魔法数字(莫名其妙出现的硬编码数字)，复杂函数添加注释；
c) 分支管理：保护main/master分支，组长负责合并与Code Review，成员独立分支开发；
d) 文档维护：编写README.md，包含更新日志、Todo-List(项目待办清单, Done-已实现, Todo-计划要做的功能, Optimize-可以优化改进的地方)、架构说明、BUG记录。

### 8. 程序主要功能简图

```text
[系统启动/登录鉴权]
      │
      ├─▶ 患者端 ─▶ 挂号/排队 ─▶ 候诊签到 ─▶ [医生接诊]
      │                                ├─ 开具检查 → 检查科登记 → 报告回传
      │                                └─ 开具处方 → [药房系统] → 审核发药 → 患者取药
      │
      ├─▶ 医护/管理端 ─▶ 发起住院证 → [住院管理中心] ─▶ 床位分配/押金 → 病区报到
      │                        │                              ├─ 日常查房/治疗记录
      │                        │                              └─ 出院结算 → 床位释放/消毒
      │
      └─▶ 全局数据中心 ─▶ 基础字典维护 / 权限管控 / 多维报表 / 预测分析 / 数据备份恢复

```

## 特殊说明

(1) 医疗管理系统至少容纳 100 名普通门诊患者, 30 名住院患者, 20 名医生, 5 个科室 (每个科室至少 3 名医生), 3 种病房 (部分科室与病房相关), 至少 20 类药品 (部分药品与科室相关); 且以上情况需要考虑人名重名、存储长度, 药品名长度、商品名、通用名、别名等因素的影响。

(2) 医疗管理系统至少包含: ①患者信息和医疗记录管理: 患者个人信息、相关联的医疗记录, 医疗记录至少包括“挂号、看诊、检查、住院”4类和此类医疗记录的必要信息; ②住院病房和床位管理: 至少包含病房类型和关联科室、床位状态管理, 患者与床位的关联; ③药房和药品管理: 药品出入库管理、处方发药管理。

(3) 医疗管理系统要能够提供各种报表，考虑不同情况下、不同权限人员需要查看的报表信息：如按照患者、医生、科室、药品名等基础信息查询；适用于医护视角、管理视角和患者视角的统计查询。

(4) 试验班学生, 除 (1) ~ (3) 中所述基本功能外, 还应该实现数据分析功能（如根据历史数据的分析和预测功能, 根据住院情况优化不同科室的病房分配、提高病房的利用率）, 并能以多种形式展示上述统计和分析结果。
