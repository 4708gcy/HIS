# 郭承宇

**求职意向：后端开发工程师（暑期实习）**

📱 15047894708 | ✉️ gcy_4708@qq.com | 🐙 [GitHub](https://github.com/4708gcy)

---

## 🎓 教育背景

**吉林大学**（985 / 双一流） | 软件工程（本科） | 2023.08 - 2028.06（预计）

- **GPA：** 3.56 / 4.0 | **专业排名：** 40 / 350（前 11.4%）
- **相关课程：** 数据库系统、操作系统、计算机网络、数据结构、面向对象程序设计、算法设计与分析

---

## 🛠 技术技能

| 类别 | 技能 |
|------|------|
| 后端开发 | Python FastAPI / Flask、RESTful API 设计、MySQL 8.0（表设计、事务、C API）、Pydantic 数据校验 |
| LLM & RAG | LangChain、OpenAI 兼容 API、faiss 向量检索、sentence-transformers Embedding、动态 Few-shot Prompt 工程 |
| 系统编程 | C/C++17、WinSock2 网络编程、Win32/Linux 跨平台兼容、链表/模板元编程 |
| 数据与算法 | pandas 数据分析、matplotlib 可视化、Holt-Winters 时序预测、Z-score 异常检测、TF-IDF 文本相似度 |
| 工程工具 | Git、CMake、Docker、VS Code、pytest 单元测试、YAML 配置管理 |

---

## 💻 项目经验

### HIS 医院信息系统 | 独立开发

**技术栈：** C++17 / MySQL 8.0 / WinSock2 / Python FastAPI + LangChain + faiss / CMake / Docker

**项目简介：** 医院信息管理系统，采用 C++17 控制台 + MySQL 8.0 持久化架构，覆盖 5 类用户角色和 8 种医疗业务记录的全生命周期管理；集成 Python FastAPI AI 微服务，提供 LLM 增强的需求预测、异常检测、床位优化和 RAG 知识库问答能力。

**仓库地址：** [https://github.com/4708gcy/HIS](https://github.com/4708gcy/HIS)（26,000+ 行 C++ / 1,800+ 行 Python）

**核心工作：**

- **AI 智能分析微服务（FastAPI + LangChain）：** 使用 FastAPI 搭建 RESTful API 服务（9 个端点），实现传统统计（Holt-Winters / Z-score）与 LLM 推理双策略路由，支持 `traditional` / `llm` / `auto` 三种模式自动切换；基于 TF-IDF 从历史数据检索相似案例构建动态 Few-shot Prompt，通过 LangChain ChatPromptTemplate 与 OpenAI 兼容 LLM 交互；实现 RAG 知识库问答（sentence-transformers 向量化 + faiss 索引 + 滑动窗口分块）；C++ 端通过 WinSock2 手工构造 HTTP/1.1 POST 请求与微服务通信
- **数据库架构设计：** 独立设计 21 张 MySQL 数据表（14 主表 + 7 子表），建立外键级联约束（`ON DELETE SET NULL` / `CASCADE`）与 CHECK 约束保障数据完整性；实现内存链表与 MySQL 的双向同步：保存时通过事务包装 `INSERT ... ON DUPLICATE KEY UPDATE` 实现原子化 upsert，加载时通过 `SELECT MAX()` 初始化全局 ID 计数器
- **安全认证体系：** 实现 SHA-256 加盐哈希（随机 16 字节盐 + 10,000 次迭代），设计 `verifyPasswordCompat()` 向后兼容旧版低迭代哈希；API Key 三级优先级机制（环境变量 → 配置文件 → 默认值）；全部使用参数化查询防止 SQL 注入，禁用 `CLIENT_MULTI_STATEMENTS`
- **系统架构优化：** 将单体 Admin 模块（8,500+ 行）拆分为 4 个职责独立的子模块；设计 `EntityRepository<T>` 泛型模板提供 O(1) ID 查找与统一生命周期管理，通过模板特化适配不同实体的 ID 字段差异；抽象 `displayChainByFilter<T>` 泛型筛选模板消除链表遍历的重复代码
- **药物安全模块：** 设计复方药物成分级精确匹配算法（`tokenizeDrugName` 分词 + `isConstituentOf` 逐成分匹配），解决子串匹配的假阳性问题（如"阿莫西林"误匹配"阿莫西林克拉维酸钾"）；实现 30 对药物相互作用检测 + 患者过敏史交叉校验

**项目成果：**

- 从零独立完成数据库设计、核心业务逻辑、AI 微服务全栈开发，代码量 28,000+ 行
- AI 服务支持 Docker 容器化部署，提供统一 JSON 响应格式与 Pydantic 数据校验
- 系统具备完整的事务管理、异常恢复（信号处理器紧急落盘）与跨平台兼容能力

---

## 🏆 荣誉奖项

- **校级二等奖学金** | 吉林大学
- **院优秀学生** | 吉林大学

---

## 📝 自我评价

- **快速学习与技术落地：** 在 HIS 项目中独立掌握 MySQL C API 原生开发、WinSock2 网络编程、FastAPI 微服务搭建、LangChain LLM 集成等技术并落地到项目级水平
- **架构设计与代码质量意识：** 主动将单体模块拆分重构，引入泛型模板统一实体管理，编写结构化项目文档（CLAUDE.md），具备代码可维护性的工程思维
- **问题驱动的解决方案能力：** 善于从具体业务问题出发设计技术方案，如针对复方药物误匹配问题设计分词+精确匹配算法，针对数据分析灵活性需求设计多策略路由架构
